environment:
  matrix:
    - nodejs_version: 0.11

platform:
  - x86

install:
  - node --version
  - npm --version
  - node -e "console.log('node version ' + process.version + ', architecture ' + process.arch);"
  - npm install
  - npm test

build: OFF
test: OFF
deploy: OFF
